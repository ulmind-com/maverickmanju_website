# Frontend environment. Copy to .env (a local .env is already committed with the dev default).
# In production set this to the deployed FastAPI URL, e.g. https://api.maverickmanju.in
VITE_API_URL=http://localhost:8000
